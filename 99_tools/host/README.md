# Host tools (not flight)

| Tool | Notes |
|------|-------|
| `l0_smoke_main.cpp` | Compile/link check for `bd_inner_loop`. Synthetic I/O — not HIL. |

Build separately against `05_avionics/flight_software` includes; do not ship in flight image.
