# Avionics bench — ALPHA

Rack with Compute A, Compute B, FC/L0, dual DC-DC, ESC **load banks** (resistive / dyno — not fake motors inventing thrust telemetry). Inject real heartbeats between A/B and run `10_tests/hil/TEST_PLAN_ALPHA.md`.
