# Soft runtime — BLOCKED

No soft twin / sensor_mocks / fake HIL runners in this tree.

**Allowed later:** launch files wiring **real** Gazebo/AirSim/hardware drivers to `digital_twin/topic_map.yaml`.

**BLOCKED without:** camera/IMU/GNSS/LiDAR drivers (or real sim plugins), real `detector_alpha.onnx`, L0 fed by real IMU.
