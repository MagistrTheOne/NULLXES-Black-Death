# Blended wing sections

**BLOCKED** — regenerated CSVs only after `scripts/blend_sections.py` validates closed, monotonic chord stations.

Prior bin-export artifacts removed (duplicate x-stations / nonphysical thickness). Do not treat this folder as geometry source of truth until validation passes.
