# CERBER detect — extras only for train pods (NOT flight image).
# Do NOT pin torch / torchvision / python here — RunPod PyTorch images ship them
# (e.g. runpod/pytorch:*-cu128*-torch280-ubuntu2404 → Torch 2.8 + CUDA).
#
# On pod after clone:
#   pip install -r 06_autonomy/models/requirements-train.txt

ultralytics>=8.0
huggingface_hub>=0.23
PyYAML>=6.0
# optional: HF datasets API for some hubs
datasets>=2.19
