# CERBER V2 pursuit boost — Ultralytics train (RunPod bare)
project: cerber-detect
run_name: v2-pursuit
# Prefer local v2 FT best; else Hub MagistrTheOne/CERBER-CV-v2 (has best.pt)
base_weights: runs/detect/cerber-detect/v1-uav-ft/weights/best.pt
hub_fallback: MagistrTheOne/CERBER-CV-v2
data_yaml: data.yaml
imgsz: 1280
epochs: 60
batch: 16
device: 0
workers: 8
export_imgsz: 640
export_opset: 17
flight_config: configs/detector_alpha_v2b.yaml
flight_onnx: ../../onnx/detector_alpha_v2b.onnx
