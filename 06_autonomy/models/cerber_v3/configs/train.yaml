# CERBER V3 — power_line FT on v2b. Do not overwrite v2b.
project: cerber-detect
run_name: v3-power
base_weights: runs/detect/cerber-detect/v2-pursuit/weights/best.pt
hub_fallback: MagistrTheOne/CERBER-CV-v2
data_yaml: data.yaml
imgsz: 1280
epochs: 50
batch: 12
device: 0
workers: 8
export_imgsz: 640
export_opset: 17
flight_config: configs/detector_alpha_v3.yaml
flight_onnx: ../../onnx/detector_alpha_v3.onnx
